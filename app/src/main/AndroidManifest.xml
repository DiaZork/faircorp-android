<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faircorp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faircorp">
        <activity android:name=".HeaterActivity"></activity>
        <activity android:name=".RoomsActivity" />
        <activity android:name=".HeatersWindows" />
        <activity
            android:name=".WindowActivity"
            android:parentActivityName=".WindowsActivity" />
        <activity android:name=".MenuWindows" />
        <activity
            android:name=".WindowsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>